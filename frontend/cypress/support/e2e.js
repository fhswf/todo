beforeEach(() => {
    // Diese Meldung wird vor jedem Test in jedem Spezifikationsfile geloggt
    cy.log('I run before every test in every spec file');
    
    //cy.visit('http://localhost:3000');
});
